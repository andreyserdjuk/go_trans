Wordpress plugin: send POST-requests to google translate service (GET-requests allows only a small amount of text) and allows you create multiple multilangual posts from one.

It works without Google API.
